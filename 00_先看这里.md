# A股因子挖掘系统

这是一套面向人工操作的因子候选生成、筛选、复算和冻结工具。系统交付源码，不生成策略组合，不承诺未来收益。每条入选因子都保留公式、原子、方向、原始值、横截面排名、最终分数、筛选记录和淘汰原因。

## 最短运行路径

1. 安装 Python 3.11 或更高版本。
2. 在本目录运行 `python 安装与检查.py`。
3. 复制 `配置/正式研究模板.json`，填写字段名、日期边界和输入文件。
4. 先运行资源预估：`python 运行因子挖掘.py 资源预估 --配置 配置/我的研究.json --行数 2500 --证券数 3000`。
5. 再运行研究：`python 运行因子挖掘.py 正式挖掘 --配置 配置/我的研究.json --面板 数据/研究面板.parquet --标签列 future_return`。
6. 2025与2026的逐日证据准备完成后，运行：`python 运行因子挖掘.py 年度复核 --证据 输出/逐日稳定性证据.parquet --输出 输出/年度复核.csv`。

## 硬边界

信号时钟、入场偏移、持有期和标签收益类型由配置显式声明。多日收益使用错位持仓队列计算，purge 长度包含入场偏移和持有期。缺少2025或2026数据时，年度闸门返回“尚未完成全部年度验证”，不会自动通过。

系统中的因子收益与年化指标属于研究评价，不等于真实账户收益。A股涨跌停、停牌、T+1、整手、印花税和公司行动由独立执行适配器处理，但本交付不把因子自动拼成策略。

## 关于77%的说明

“经过后续处理的候选在特定历史复现口径下约有77%保持延续”来自卖家提供的独立历史复现记录。该数字不由源码内置数据生成，不属于本系统的普遍成功率，也不构成收益保证。它只能说明规范筛选、去重和再处理具有研究价值。使用方应使用自己的数据、字段和时间边界重新计算。

完整说明见 `说明/完整使用手册.md`。

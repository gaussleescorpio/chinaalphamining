# 扩展接口

可以扩展四类内容：数据适配器、数学原子、公式插件、评价插件。扩展必须声明输入字段、单位、回看窗口、最晚可用时间、可能的相反解释和失败条件。

数据适配器负责把Tushare、自建数据库或Parquet目录转换成标准长表。数学原子负责可见历史信息的确定性变换。公式插件只产生规范公式树。评价插件可以增加HAC、时间块Bootstrap、收益集中度、稳定性和容量指标，但不得绕过基础漏斗。

残差信息发现采用扩展时间交叉拟合：每一折只用更早日期建立基线，再对基线误差寻找解释变量。残差用于发现新信息，不作为最终策略，也不要求使用方部署残差模型才能计算冻结因子。
